specVersion: "v1beta"
serviceId: "checklist-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "checklist-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
